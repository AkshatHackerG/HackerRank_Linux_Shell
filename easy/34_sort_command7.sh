#!/bin/bash 

#sort command is used for sorting, -r for reverse, -n for sorting numbers, -k2 flag used for sorting things starting and ending column 2, -t$'\t' is used to delimit tab.
sort -rnk2 -t$'|' 
